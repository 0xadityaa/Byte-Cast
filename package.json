{
  "name": "byte-cast",
  "version": "1.0.0",
  "description": "A browser based app to process and send camera stream to social-media's RTMP servers for streaming.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js"
  },
  "author": "Aditya Negandhi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
